# Contoh file environment variables
# Jika Anda ingin menggunakan Weatherstack sebagai provider utama, masukkan API key di sini.
# Jangan commit file ini jika berisi kunci nyata.

# API key for MediaStack (berita). Set this in your local `.env` when running the server.
# Example: MEDIASTACK_KEY=your_mediastack_api_key_here
WEATHERSTACK_KEY=your_weatherstack_api_key_here

# API key for MediaStack (berita). Set this in your local `.env` when running the server.
# Example: MEDIASTACK_KEY=your_mediastack_api_key_here
MEDIASTACK_KEY=your_mediastack_api_key_here
